services:
  mongodb:
    image: mongo:7
    container_name: mongodb-patterns
    ports:
      - "27018:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: "N05@ssword"
    command: ["mongod", "--replSet", "rs0", "--bind_ip_all"]
    volumes:
      - mongodb-data:/data/db
      - ./scripts/mongo-init-replica.sh:/docker-entrypoint-initdb.d/init-replica.sh:ro
    healthcheck:
      test: >
        mongosh --quiet --username admin --password 'N05@ssword' --authenticationDatabase admin
        --eval "try { rs.status().ok } catch(e) { 0 }" | grep -q 1
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 30s

volumes:
  mongodb-data:
